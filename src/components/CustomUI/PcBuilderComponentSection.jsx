const PcBuilderComponentSection = ({ children }) => {
    return <div className="bg-secondary text-primary px-5 py-1 rounded-sm font-semibold">{children}</div>;
};

export default PcBuilderComponentSection;
